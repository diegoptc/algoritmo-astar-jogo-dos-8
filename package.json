{
  "name": "jogo-dos-8",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && cp src/index.html dist/ && mkdir -p dist/css && cp src/css/* dist/css/ && cp src/js/lodash.js dist/js",
    "start": "pnpm run build && cd dist/ && python3 -m http.server 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.2.2"
  }
}
