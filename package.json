{
  "name": "jogo-dos-8",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/ && mkdir -p dist/css && cp src/css/* dist/css/ && cp src/js/lodash.js dist/js",
    "start": "tsc && node dist/js/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.2.2"
  }
}
